const LoadingPage = () => {
  return (
    <div style={{ padding: 24 }}>
      <div style={{ height: 6, borderRadius: 8, background: 'linear-gradient(90deg, #dbeafe, #bfdbfe, #dbeafe)' }} />
    </div>
  );
};

export default LoadingPage;
