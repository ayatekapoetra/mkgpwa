import PurchasingRequestPage from "views/scm/purchasing-request";

export const metadata = {
  title: "Purchasing Request",
};

/** Connects the Purchasing Request list route to its feature view. */
export default function Page() {
  return <PurchasingRequestPage />;
}
