'use client';

import CheckerPagePlaceholder from '../components/CheckerPagePlaceholder';

export default function DailyCheckerPitCreateView() {
  return (
    <CheckerPagePlaceholder
      heading="Create Checker PIT"
      routeTitle="Daily Checker PIT"
      backUrl="/daily-checker-pit"
      description="Form create Checker PIT akan diimplementasikan pada tahap berikutnya dengan field sesuai PRD (date_ops, shift, excavator, dumptruck, material, seq, pit, driver)."
    />
  );
}
